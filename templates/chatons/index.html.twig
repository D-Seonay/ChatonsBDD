{% extends 'base.html.twig' %}

{% block title %}Chaton index{% endblock %}

{% block body %}
    <h1>Chaton index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom</th>
                <th>Photo</th>
                <th>Categorie</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for chaton in chatons %}
            <tr>
                <td>{{ chaton.id }}</td>
                <td>{{ chaton.nom }}</td>
                <td>{{ chaton.categorie.titre }}</td>
                <td>{{ chaton.Photo }}</td>
                <td>

                    <a href="{{ path('app_chatons_show', {'id': chaton.id}) }}" class="btn btn-secondary">show</a>
                    <a href="{{ path('app_chatons_edit', {'id': chaton.id}) }}" class="btn btn-danger">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_chatons_new') }}" class="btn btn-primary">Create new</a>
{% endblock %}
